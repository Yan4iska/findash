# Root-level environment notes
# Copy app-specific env files before running locally:
#   cp apps/api/.env.example apps/api/.env
#   cp apps/web/.env.example apps/web/.env
#
# MongoDB must be running for the API (see README.md).
